# Copy this file to ".env" (in the server/ folder) and paste your real key.
# The .env file is gitignored and is read ONLY by the server — it never reaches
# the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the proxy port (defaults to 8787).
# PORT=8787
